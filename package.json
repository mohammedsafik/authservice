{
  "name": "authservice",
  "version": "1.0.0",
  "description": "Production-ready authentication service built with Node.js, Express, MongoDB, Mongoose, JWT, and bcrypt.",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check seed.js && node --check config/env.js && node --check config/db.js && node --check models/User.js && node --check services/authService.js && node --check controllers/authController.js && node --check routes/authRoutes.js && node --check middleware/asyncHandler.js && node --check middleware/authMiddleware.js && node --check middleware/errorMiddleware.js && node --check middleware/notFoundMiddleware.js && node --check middleware/loggerMiddleware.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
